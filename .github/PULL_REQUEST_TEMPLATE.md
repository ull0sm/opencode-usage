## What & why

<!-- Describe the change and the problem it solves. Link related issues ("Fixes #123"). -->

## How tested

<!-- How did you verify? e.g. manual steps on seeded data, new checks, npm run build -->

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] README/docs updated if user-facing behavior changed (commands, env vars, formats)
- [ ] No real usage data, `.env*`, `data/` contents or secrets included
- [ ] Imports stay idempotent if record shapes changed (`source_ref` / content-hash dedupe)
